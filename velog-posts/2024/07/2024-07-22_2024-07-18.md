---
title: "2024-07-18"
date: 2024-07-22 17:46:25
source: "https://velog.io/@mi_nini/2024-07-18"
---

#### 오늘 배운 내용은 pandas(2) 라이브러리다. (위 소스코드는 실습한 내용을 포함한다)

들어가기 전 간단하게 pandas를 알아보고 가자  
pandas는 쉽고 직관적인 관계형 또는 분류된 데이터로 작업할 수 있도록 설계된 빠르고 유연하며 표현이 풍부한 데이터 구조를 제공하는 python 패키지이다.

## 판다, 하는 이유?

  1. 파이썬의 데이터 조작 기능을 강화  
파이썬은 범용 프로그래밍 랭귀지입니다. 소프트웨어 개발을 위해서는 익히기도, 이해하기도 쉽게 제작되었지만, 본격적인 데이터 조작과 분석을 위해서는 어려움이 많습니다. 하지만 파이썬에 판다 그를 붙여서 사용하게 되면 데이터 조작에 특화된 함수를 사용하여 데이터 조작과 분석이 한결 쉬워지게 됩니다. 또한 파이썬 만으로는 구현하기 어려운 조인(join)이나 그룹 피아(group-by) 기법 등도 사용할 수 있습니다.
  2. 대표적 데이터 관련 라이브러리  
판다 쓰는 파이썬에서 데이터를 조작함에 있어 대표적인 라이브러리로 자리 잡았습니다. 그렇기 때문에 다른 사람이 판다 스스로 작성한 코드를 공유하여 쉽게 활용할 수도 있습니다.
  3. 데이터 프레임 조작에 특화  
데이터 프레임(dataframe)은 데이터 조작과 분석에는 표준과 같은 구조입니다. 판다 쓰는 R의 데이터 프레임 구조를 차용하여 사용합니다. 데이터 프레임을 인덱싱(indexing)하여 사용하면 데이터 조작과 분석이 보다 용이하게 됩니다.  
![](https://velog.velcdn.com/images/mi_nini/post/adfd44b5-158e-4318-96f3-52ebc783cc7f/image.png)



## pandas는 크게 2가지의 데이터 구조를 가진다.

1차원 데이터를 다루는데 효과적인 Series, 행과 열로 구성된 2차원 데이터 DataFrame으로 나뉜다. (그 외에 시계열 데이터와 Indexing&Slicing도 있다.) 추가, 수정, 삭제가 가능한 특징도 갖고 있다.  
다시 돌아와 오늘 배운 내용은 크게 4가지이다.  
1\. 데이터 살펴보기  
2\. 누락된 값 처리하기  
3\. 데이터 분석을 위한 함수들  
4\. 간단한 시각화 및 공공데이터 read_csv 해보기 (+ 공부 사이트 URL)

### #데이터 정보 살펴보기

몇 개의 행만 출력하기 df.head() / df.tail()

데이터에 대한 정보 얻기 df.info()

DataFrame의 계산 가능한(수치 데이터 칼럼) 통계 보기 df.describe()

### 데이터 내용 살펴보기

Series 내의 데이터 개수(빈도수) 세기 df['column'].value_count()

Series 내의 unique 한 값 보기 df['column'].unique()

원하는 값의 존재 유무 df['column'].isin(['alpha', 'beta'])

'''  
head -> 상위 5개 출력  
tail -> 하위 5개 출력  
info -> Pandas 데이터 프레임의 요약 정보를 제공 ( 데이터 프레임의 열 수, 행 수, 열 이름, 데이터 유형, 비어 있지 않은 값의 수 등등 같은 정보를 제공함)  
shape -> 행과 열의 개수를 튜플로 반환 ( ex -> (a, b) a -> 행 b -> 열 )  
'''  
seaborn에서 했던 데이터 가져오기 코드
[code] 
    import seaborn as sns
    
    df = sns.load_dataset('tips') # 데이터 가져오기
    df.describe() #통계데이터 볼 수 있다.
    df
[/code]

## 2\. 데이터 분석을 위한 함수들

1) Summarize Data  
▪ len(df) -> 항목 개수  
▪ sum(), count()  
▪ median(), min(), max(), mean(), var(), std(), corr(), cov() -> 엑셀에서 쓰던 함수 값들과 유사함

2) Reshape Data  
▪ pd.concat([df1, df2], axes=0)  
▪ pd.merge(df_left, df_right, left_on=None, right_on=None, on=None,  
how='inner')  
▪ df.pivot_table(index=‘var’, columns=‘var’, values=‘var’,  
aggfunc=‘func’)  
▪ df.sort_values(‘mpg’, ascending=False)  
▪ df.rename(columns={‘y’:‘year’})  
▪ df.drop() df.drop_duplicates(inplace=True)

3) Group Data  
▪ df.groupby(by=“col”) -> 특정 컬러 그룹화

#axis -> 0이 행에 방향을 의미하고 1이 열에 방향을 의미함  
#count() -> 유효한 데이터의 개수가 몇 개인가?_ nan 데이터를 미포함한다.
[code] 
    import numpy as np 
    import pandas as pd
    
    df1 = pd.DataFrame([['a', 1], ['b', 2]], columns=list('AB'))  #axis는 결합방향임 = 0 or = 1
    df2 = pd.DataFrame([['a', 1], ['b', 2]], columns=list('AB')) 
    df3 = pd.concat([df1, df2], ignore_index=True) #ignore_index가 true 라면 원래 순서를 무시하고 이어서 작성
    print(df3)하세요
[/code]

### 개념들을 완벽하게 다시 숙지하고 가야 함 -> 데이터베이스에서 배웠던 내용들임

pd.merge(df_left, df_right, left_on=None, right_on=None, on=None, how='inner')

▪ 두 DataFrame을 각 데이터에 존재하는 고윳값(key)을 기준으로 조인

• join 방식 : inner, left, right, outer

• 고윳값(key) 칼럼이 같을 때,  
− pd.merge(df_left, df_right, on='key', how='join_method')

• 고윳값(key) 칼럼이 다를 때,  
− pd.merge(df_left, df_right, left_on='lkey', right_on='rkey', how='join_method')

![](https://velog.velcdn.com/images/mi_nini/post/885956f3-2d3a-4ec8-8641-9d0f8a74e1e7/image.png)
[code] 
    import numpy as np 
    import pandas as pd
    
    df1 = pd.DataFrame([['a', 1], ['b', 2], ['c', 3]], index=[0, 1, 2],
    columns=list('AB'))
    df2 = pd.DataFrame([['a', 1], ['b', 2], ['c', 3]], index=[1, 2, 3], 
    columns=list('AB'))
    df3 = pd.concat([df1, df2], axis=1) # outer join 중복되는 부분을 제외한 부분을 보여줌
    print(df3)
[/code]
[code] 
    import numpy as np 
    import pandas as pd
    
    df1 = pd.DataFrame([['a', 1], ['b', 2], ['c', 3]], index=[0, 1, 2], 
    columns=list('AB'))
    df2 = pd.DataFrame([['a', 1], ['b', 2], ['c', 3]], index=[1, 2, 3], 
    columns=list('AB'))
    df3 = pd.concat([df1, df2], axis=1, join='inner')  #중복되는 부분만 보여줌
    df3
[/code]

### 피벗, 피벗테이블
[code] 
    import numpy as np 
    import pandas as pd
    
    df = pd.DataFrame({'foo': ['one', 'one', 'one', 'two', 'two', 'two'],
    'bar': ['A', 'B', 'C', 'A', 'B', 'C'], 
    'baz': [1, 2, 3, 4, 5, 6],
    'zoo': ['x', 'y', 'z', 'q', 'w', 't']})
    #print(df, '\n')
    print(df.pivot(index='foo', columns='bar', values='zoo'))
[/code]
[code] 
    import pandas as pd 
    import seaborn as sns
    
    df = sns.load_dataset('titanic')
    df = df[['age', 'sex', 'class', 'fare', 'survived']]
    df1 = pd.pivot_table(df,             # 피벗할 데이터프레임
                         index= 'class', # 행 위치에 들어갈 열
                         columns='sex',  # 열 위치에 들어갈 열
                         values='age',   # 데이터로 사용할 열
                         aggfunc='mean') # 데이터 집계함수
    df1
    
    df2 = pd.pivot_table(df, index= 'class', columns= 'sex', values= 'survived', aggfunc= ['mean', 'sum'])
    
    df2
    
    '''python
    3등석(Third)에   탄   여자(female)들의   생존여부 
    (survived)는    평균    50%정도,
    1등석(First)에    탄    남자(male)들의    생존자는    모두 
    45명
    '''
    
    df3 = pd.pivot_table(df, index = ['class','sex'], columns = 'survived',
    values = ['age','fare'], aggfunc = ['mean','max'])
    
    df3
[/code]

-> 결괏값을 보고 데이터를 정확하게 분석할 줄 알아야 한다.(가공되지 않은 데이터가 많기 때문에 내가 레이블을 만들고 가공하기 위해서는 사전에 데이터를 정확하게 분석하고 어떤 것을 이용해야 할지 알아야 한다. 그렇기에 데이터 분석에 대한 공부가 중요하다.

### 그룹화
[code] 
    import numpy as np 
    import pandas as pd
    
    data = {'Name': ['Lee', 'Kim', 'Lee', 'Kim', 'Kim'],
    'Year': [2013, 2013, 2015, 2015, 2015], 
    'Points': [1.5, 1.8, 3.6, 2.4, 2.9]}
    df = pd.DataFrame(data) 
    print(df, '\n')
    print(df.groupby(['Name', 'Year']).mean())
[/code]

  4. 간단한 시각화  
'''  
• data : Series or DataFrame  
• x : label or position(only DataFrame)  
• y : label or position(only DataFrame)  
• kind : {line, bar, barh, hist, box, kde, density, area, pie, scatter, hexbin}  
• title : str  
• grid : bool(default: None)  
• legend : bool  
• xticks, yticks : sequence  
• xlebel, ylabel : str



### ▪ 참고 사이트 (중요)

• <https://pandas.pydata.org/docs/user_guide/visualization.html#>  
❖ 공공데이터 포털  
▪ <https://www.data.go.kr/index.do>  
❖ 기상청 기상자료개방 포털  
▪ <https://data.kma.go.kr/cmmn/main.do>  
❖ Pandas를 이용한 데이터 분석 기초  
▪ <https://wikidocs.net/2873>  
❖ Pandas Tutorials  
▪ <http://pandas.pydata.org/pandas-docs/stable/tutorials.html>  
❖ Pandas documentation  
▪ <https://pandas.pydata.org/docs/index.html>

➢ 자유로운 plotting을 위해 보통 matplotlib 또는 seaborn을 사용하는 것  
이 바람직하다고 함
